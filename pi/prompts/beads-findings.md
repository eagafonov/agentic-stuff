---
description: Record code review findings as beads issues
argument-hint: "[labels: comma-separated labels to apply to created issues]"
---
Record findings from the current conversation as beads issues.

For each finding, create a beads issue using `bd create` with an appropriate title and description.

If labels are provided below, apply them to every created issue using the `--labels` flag.

Labels to apply (use if provided, ignore if empty): $@
